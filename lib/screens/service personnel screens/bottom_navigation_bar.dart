import 'package:flutter/material.dart';

enum BottomNav { home, profile, requests, more }

class ServicePersonnelBottomNavBar extends StatelessWidget {
  const ServicePersonnelBottomNavBar({super.key});

  @override
  Widget build(BuildContext context) {
    return BottomNavigationBar(items: []);
  }
}
